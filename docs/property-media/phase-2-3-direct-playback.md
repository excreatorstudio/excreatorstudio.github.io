# Phase 2.3 / 2.4

Phase 2.3: Orbit poster buttons select and open the existing PropertyMediaLightbox in one activation. Native buttons handle Enter/Space. The track's existing 8px click slop and 48px switch threshold remain. Horizontal movement beyond slop, vertical scroll gestures, cancellation and lost capture suppress pointer clicks. Keyboard activation is independent of pending pointer suppression. Ordinary portfolio cards already open directly.

Phase 2.4 model asset status: **NOT FOUND**. Checked Property Media model, image and media directories in the integration checkout and parent project. No verified GLB/glTF is available; no model URL or 3D dependency was added.

**TRUE 360 BLOCKED — REAL MODEL ASSET REQUIRED.** FloorPlanShowcase stays in image mode with the approved transparent PNG and bounded 2.5D interaction. The section exposes a nonvisual `data-model-status` contract. There is no active model adapter or claimed camera orbit.

Next model integration requires a supplied GLB/glTF with verified external buffers/textures and materials. Reuse the single FloorPlanShowcase entry point, load on approach to viewport, retain the PNG during loading/error, and validate camera limits and room targets from the actual model. Do not infer geometry from the PNG.
